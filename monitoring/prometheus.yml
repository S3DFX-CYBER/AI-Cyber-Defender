global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: tenet-analyzer
    static_configs:
      - targets: ['analyzer:8100']
    metrics_path: /metrics
